import ReactDOM from "react-dom";
import "./styles.css";

import App from "./App";

ReactDOM.render(
    // What we want to see rendered
    <App />, 

    // Where we want to see it rendered
    document.getElementById("root")
);
